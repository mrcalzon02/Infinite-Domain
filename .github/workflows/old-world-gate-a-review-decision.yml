name: Old World Gate-A Review Decision

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'scripts/record_old_world_gate_a_review.py'
      - 'old_world_narrative/reviews/heavy_rebuild/OWS-*_GATE_A_R1_REVIEW.md'
      - '.github/workflows/old-world-gate-a-review-decision.yml'

permissions:
  contents: write

concurrency:
  group: old-world-static-build-main
  cancel-in-progress: true

jobs:
  record-gate-a-decision:
    runs-on: ubuntu-latest
    steps:
      - name: Check out exact review revision
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Compile guarded Gate-A recorder
        run: python -m py_compile scripts/record_old_world_gate_a_review.py

      - name: Record already-completed Gate-A visual decision
        run: python scripts/record_old_world_gate_a_review.py

      - name: Persist Gate-A decision state
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -- old_world_narrative/registry/heavy_rebuild_state.json
          if git diff --cached --quiet; then
            echo 'No Gate-A state change to persist.'
          else
            git commit -m 'Record active Old World Gate-A review decision'
            git fetch origin main
            git rebase origin/main
            git push origin HEAD:main
          fi
