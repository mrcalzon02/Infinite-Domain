name: OWS-004 Gate-A Massing Review

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - '.github/workflows/old-world-ows004-gate-a.yml'

permissions:
  contents: write

concurrency:
  group: old-world-static-build-main
  cancel-in-progress: true

jobs:
  render-gate-a:
    runs-on: ubuntu-latest
    steps:
      - name: Check out exact massing revision
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install render dependency
        run: python -m pip install --disable-pip-version-check pillow

      - name: Compile Gate-A review model
        run: |
          python -m py_compile \
            scripts/generate_wasteland_sites.py \
            scripts/render_structure_review.py \
            scripts/render_old_world_heavy_rebuild_review.py \
            scripts/render_ows004_gate_a_massing.py

      - name: Render OWS-004 Gate-A r1
        run: python scripts/render_ows004_gate_a_massing.py

      - name: Record Gate-A workflow provenance
        run: |
          mkdir -p old_world_narrative/reviews/heavy_rebuild/visual/OWS-004
          cat > old_world_narrative/reviews/heavy_rebuild/visual/OWS-004/gate_a_workflow_run.json <<EOF
          {
            "workflow": "OWS-004 Gate-A Massing Review",
            "run_id": "${GITHUB_RUN_ID}",
            "run_attempt": "${GITHUB_RUN_ATTEMPT}",
            "head_sha": "${GITHUB_SHA}",
            "active_target": "OWS-004"
          }
          EOF

      - name: Upload Gate-A review artifact
        uses: actions/upload-artifact@v4
        with:
          name: old-world-heavy-rebuild-OWS-004-gate-a-r1
          retention-days: 7
          path: |
            old_world_narrative/reviews/heavy_rebuild/visual/OWS-004/
            old_world_narrative/registry/heavy_rebuild_state.json

      - name: Persist Gate-A artifacts and pending-review state
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -- \
            old_world_narrative/reviews/heavy_rebuild/visual/OWS-004/ \
            old_world_narrative/registry/heavy_rebuild_state.json
          if git diff --cached --quiet; then
            echo 'No Gate-A changes to persist.'
          else
            git commit -m 'Persist OWS-004 Gate-A massing review artifacts'
            git fetch origin main
            git rebase origin/main
            git push origin HEAD:main
          fi
