name: Old World Intact Planning Decision

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'scripts/record_old_world_intact_planning_passes.py'
      - 'old_world_narrative/reviews/heavy_rebuild/OWS-*_PASS12_VCF_IDENTITY.md'
      - '.github/workflows/old-world-intact-planning-decision.yml'

permissions:
  contents: write

concurrency:
  group: old-world-static-build-main
  cancel-in-progress: true

jobs:
  record-intact-planning:
    runs-on: ubuntu-latest
    steps:
      - name: Check out planning revision
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Compile guarded intact-planning recorder
        run: python -m py_compile scripts/record_old_world_intact_planning_passes.py

      - name: Record completed Passes 7-12
        run: python scripts/record_old_world_intact_planning_passes.py

      - name: Persist Gate-B readiness
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -- old_world_narrative/registry/heavy_rebuild_state.json
          if git diff --cached --quiet; then
            echo 'No intact-planning state change to persist.'
          else
            git commit -m 'Record active Old World intact planning passes'
            git fetch origin main
            git rebase origin/main
            git push origin HEAD:main
          fi
