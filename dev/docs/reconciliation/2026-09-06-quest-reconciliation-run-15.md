# Quest Reconciliation Run 15

Authoritative branch: `main`
Baseline observed before this run: `ccebd5ba9facebeb33058243008529cadaee8697`

## Authority and execution discipline

The named persistent `AI_Project_Manager.md` was searched in the connected Library by exact and close title but did not surface. Do not substitute an unrelated fuzzy match as authority. Continue under the already-established repository rules: authoritative `main`, direct source inspection before mutation, intended behavior repaired rather than bypassed, and operational completion claims only after repository read-back.

The legacy coherence module still contains obsolete root-level `docs/...` oracle/output constants and silent-empty missing-file loaders. Current `main` already contains `dev/run_quest_tree_coherence.py`, the fail-loud corrected-path runner added at `ccebd5ba9facebeb33058243008529cadaee8697`; retain that regression barrier until the underlying parser can be safely consolidated in an executable checkout.

## Air/Sea Global Logistics — previous Nether defect reclassified

The previous reconciliation ledger treated `minecraft:stronghold` in the Nether as an obvious invalid vanilla structure/dimension pair. Current localization proves that conclusion was premature. `5E0000000000001E`, **Stronghold Beneath the Lava Sky**, explicitly states that Infinite Domain relocates the stronghold ring into the Nether and that Nether stronghold discovery is required End progression. Its predecessor deliberately maps `minecraft:stronghold` from inside `minecraft:the_nether`.

Disposition: **do not replace this objective with a Nether Fortress.** The remaining work is implementation provenance: prove that the pack's shipped worldgen/configuration actually relocates the stronghold structure set into the Nether. Searches of indexed repository content and path trees did not yet expose an unambiguous `stronghold`, `strongholds`, or `concentric_rings` implementation. Absence from those searches is not proof of absence because the behavior may be supplied by mod configuration or registry scripting. The objective is therefore an integration-verification item, not a confirmed bad quest ID.

## The Rot — bypass count corrected and defect reconfirmed

Source: `config/ftbquests/quests/chapters/the_rot_spore_threat_dossier.snbt`.

The old shorthand "six AE2/Cyberware bypasses" counted milestone pairs and obscured the actual mutation scope. There are six Rot specimen milestone quests, and each grants two unrelated cross-tree hardware items in addition to ordinary Numismatics compensation. The authoritative repair scope is therefore **12 individual hardware reward objects**:

1. `5F10000000000007`: `ae2:charger`; `createcybernetics:eyeupgrades_biomonitor`.
2. `5F1000000000000F`: `ae2:item_storage_cell_1k`; `createcybernetics:eyeupgrades_targeting`.
3. `5F10000000000015`: `ae2:item_storage_cell_4k`; `createcybernetics:muscleupgrades_wiredreflexes`.
4. `5F1000000000001B`: `ae2:wireless_terminal`; `createcybernetics:brainupgrades_neuralprocessor`.
5. `5F10000000000024`: `ae2:dense_energy_cell`; `createcybernetics:organsupgrade_densebattery`.
6. `5F10000000000026`: `ae2:wireless_crafting_terminal`; `createcybernetics:brainupgrades_iceprotocol`.

The surrounding Rot dossier overwhelmingly pays Numismatics rather than technology, and the independently audited AE2/Cyberware lines use explicit civilization-era authority. These 12 rewards therefore remain confirmed progression bypasses. Repair must remove only the unrelated AE2/Create Cybernetics reward objects while preserving the Rot objectives, specimen flow, Numismatics compensation, IDs, and dependencies.

## Parallel Factory Paths — commissioning defect reconfirmed

Source: `config/ftbquests/quests/chapters/parallel_factory_paths.snbt`.

The era gates themselves remain coherent, but the chapter does not actually commission the major Immersive Engineering production multiblocks previously intended by the progression plan. `5D30000000000015` **Integrated Factory Commissioning** is only a manual checkmark. Its successor `5D30000000000016` **Dual-Path Production Proof** verifies possession of a Create Mechanical Press, an IE Voltmeter, and eight IE fluid pipes. Neither quest requires, detects, or proves operation of an Immersive Engineering Excavator or Arc Furnace.

Disposition: confirmed semantic incompleteness, not an ID/order failure. Preserve the existing quest IDs and dependencies; restore authentic Excavator and Arc Furnace commissioning/production evidence instead of adding a parallel replacement branch.

## Current mandatory repair / verification ledger

1. Remove the 12 individual Rot AE2/Create Cybernetics hardware reward objects from the six identified Rot milestones, preserving intended Rot rewards and progression.
2. Restore Excavator and Arc Furnace commissioning semantics on the existing Parallel Factory commissioning/proof IDs.
3. Verify the intended Nether stronghold relocation implementation shipped by Infinite Domain; repair worldgen/configuration if absent rather than changing the quest to a different structure.
4. Finish Air/Sea presentation normalization only after the current source is rechecked for stale checkmark/icon claims.
5. Normalize Mutant/Mekanite chapter/quest icon metadata.
6. Add the missing Stellaris chapter icon.
7. Normalize Darknet icon metadata and shape-legend usage.
8. Finish Old World availability/authority and icon/localization normalization.
9. Consolidate the legacy coherence validator's internal paths/fail-loud behavior when an executable authoritative checkout is available; until then retain the current fail-loud runner.
10. Run the complete deterministic corpus validation, including Domain Compendium, and repair all real findings before declaring existing quests closed.

## Procedural expansion candidates retained behind the correctness gate

Once the current corpus clears, strongest candidates for further depth are operational rather than possession-only: real multiblock commissioning proofs, structure-backed logistics acceptance tests, Rot countermeasure/research escalation, evidence-led structure discovery chains, and per-era demonstrations that require the player to operate the technology unlocked by that era rather than merely hold one component.
